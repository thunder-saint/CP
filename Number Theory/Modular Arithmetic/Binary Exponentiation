int power(int x, int n, int MOD) { 
    int ans = 1 % MOD;
    x %= MOD;
    if (x < 0) x += MOD;
    while (n) {
        if (n & 1) ans =  ans * x % MOD;
        x = 1LL * x * x % MOD;
        n >>= 1; 
    }
    return ans;
}
