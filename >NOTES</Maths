# if (a*b)<=n then a<=(n/b);
# in any array of n positive integers, there exists a non-empty subset whose sum is divisible by n;
# if n+1 objects are put into nboxes, then at least one box contains two or more objects;
